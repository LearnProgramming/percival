require 'spec_helper'

describe "Channel Changer plugin" do
  describe "Leaving channels" do
    it "should leave a channel when !leave-channel is called by a privilaged user" do
      
    end
    it "should not leave a channel when !leave-channel is sent by an un-privilaged user" do
      
    end
  end

  describe "Joining channels" do
    it "should join channel when !join-channel [channel] is sent by privilaged user" do

    end
    it "should not join channel when !join-channel [channel] is sent by un-privilaged user" do

    end
  end
end
